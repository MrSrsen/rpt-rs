# Build output and VCS.
target/
.git/
.github/

# Sensitive / local-only — must never enter the build context or image.
research/
.claude/
CLAUDE.md

# Not needed to build the binaries.
docs/
*.md
.gitignore
.dockerignore
Dockerfile
LICENSE

# ...except the two the image redistributes: it ships statically linked binaries, so it carries
# every dependency and both bundled font families with it, and owes their notices.
!LICENSE
!THIRD-PARTY-NOTICES.md
